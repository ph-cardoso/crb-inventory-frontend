export type TItem = {
  id: string,
  name: string,
  description: string,
  is_active: boolean,
  category_id: string,
  minimum_threshold: number,
  stock_quantity: number,
  created_at: string,
  updated_at: string
}